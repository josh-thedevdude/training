Closures
Definition: an inner function accesses the variables from its lexical scope it forms a closure.
Meaning the inner function still references the same variable in the memory until its execution context gets over and the references are garbage collected.
